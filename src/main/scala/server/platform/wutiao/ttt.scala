package server.platform.wutiao

import breeze.linalg.Axis._1

/**
  * @program: customer_model_predict_age_sex
  * @description: ${description}
  * @author: maoyunlong
  * @create: 2018-11-25 12:05
  **/
object ttt {
//  var array =Array("hello Tom","Hello tim")
//  array.flatMap(_.split(" ")).map((_,1)).groupBy(_._1).map(t=>(t._1,t._2.size)).foreach(print)
//  array.flatMap(_.split(" ")).map((_,1)).groupBy(_._1).mapValues(_.foldLeft(0)(_+_._2)).foreach(print)
  val a = "我 是"
  val b = " 你 是"

}
